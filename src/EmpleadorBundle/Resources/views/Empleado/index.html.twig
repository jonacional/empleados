{% extends 'layout/layout.html.twig' %}
{% block body %}
{{parent() }}
<div class="container">
		<div class="col-md-12">
			<div class="page-header margin-none">
				<h2 class="padding-none">Empleados</h2>
			</div>
			<div class="table-responsive">
			<table class="table table-striped table-hover">
					<thead>
						<tr>

   
      <th>Nombre</th>
      <th>Sexo</th> 
      <th>Cedula</th> 
      <th>Teléfono</th> 
      <th>Contrato</th>  
     </tr>
     </thead>
     <tbody>
     {%  for user in users  %}
      <tr>
      <td>{{ user.nombre }}</td>
      <td>{{ user.sexo }}</td> 
      <td>{{ user.cedula }}</td> 
      <td>{{ user.telefono }}</td>
      <td>{{ user.Contrato }}</td>  
      </tr>
     {%   endfor %}
     </tbody>
 </table>
 </div>
  </div>
   </div>
 {% endblock %}